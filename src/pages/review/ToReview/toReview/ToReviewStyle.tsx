import { StyleSheet } from "react-native";
import { Color } from "../../../../constants/GlobalStyle";

export const toReviewStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Color.C_white,
  },
});
